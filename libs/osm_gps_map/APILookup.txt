wrap: osm_gps_map
file: OsmGpsMap-1.0.gir

struct: MapPoint
	noCode: new_radians_with_user_data
	noCode: new_radians

struct: Map
	import: gobject.Value
	code: start
	    @property int zoom() {
	    		Value zoom = new Value;
			this.getProperty("zoom", zoom);
			return zoom.getInt();
		}
	    @property int zoom(int value) {
	    		this.setProperty("zoom", value);
			return zoom;
		}
	    @property int maxZoom() {
	    		Value zoom = new Value;
			this.getProperty("max-zoom", zoom);
			return zoom.getInt();
		}
	    @property int minZoom() {
	    		Value zoom = new Value;
	    		this.getProperty("min-zoom", zoom);
			return zoom.getInt();
		}
	    @property OsmGpsMapSource_t mapSource(OsmGpsMapSource_t source) {
			this.setProperty("map-source", source);
			return source;
	    }
	code: end
		
